{
  "name": "pdf-worker-example",
  "version": "1.0.0",
  "private": true,
  "description": "Real end-to-end example: upload a PDF to S3, save metadata in MongoDB, and a Kafka worker extracts the first 3 words into a second collection referencing the file.",
  "scripts": {
    "server": "node server.js",
    "worker": "node worker.js",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.0.0",
    "lb-upload-infra": "^0.1.1",
    "mongoose": "^8.6.0",
    "pdf-parse": "^1.1.1"
  }
}
